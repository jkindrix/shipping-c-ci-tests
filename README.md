# shipping-c-ci-tests

Scratch repository used to verify CI workflows shown in the
`shipping/` collection of the C-learning series (chapters 05–06).

The source files (`primes.h`, `primes.c`, `main.c`, `CMakeLists.txt`)
are the same minimal primes project the textual chapters build on.
The interesting content is in `.github/workflows/`.
